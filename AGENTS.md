# Refined Modal

## 项目简介

这是一个用于自定义 Obsidian 桌面端界面视觉效果的插件，主要调整各种弹窗和右键菜单的进出场动画，以及背景模糊、透明度和毛玻璃效果，让界面的打开与关闭更加流畅。

插件在 Obsidian 中显示为 **Refined Modal**，插件 ID（标识符）为 `FLRA-animation-helper`。开发目录为 `D:\AppData\obsidian-modal-blur-helper-dev`，插件运行目录为 `D:\文件\Obsidian Vault\Floratina\.obsidian\plugins\modal-blur-helper`。

## 开发与构建

在开发目录运行 `npm install` 安装依赖。运行 `npm run build` 构建一次，或运行 `npm run dev` 持续监听源码、样式和插件信息的修改并自动构建。

`esbuild.config.mjs` 将 `main.js` 编译到插件运行目录，并同步 `styles.css` 和 `manifest.json`。源码、Git 历史和开发配置均保留在开发目录。构建后在 Obsidian 中重新加载插件以应用 JavaScript 修改。

插件运行目录中的 `data.json` 保存实时用户设置，构建不得覆盖。开发目录中的 `data.json` 是迁移时保留的参数快照，继续受 Git 管理，不会自动同步实时设置。

## 主要功能

- **背景效果**：弹窗打开时，调整后方界面的模糊、变暗程度、对比度、饱和度和缩放比例，并分别控制背景进入和恢复时的动画时长与缓动曲线（动画速度随时间的变化方式）。
- **弹窗动画**：为弹窗提供淡入、淡出和缩放效果，可调整进出场时长、起始与结束尺寸，以及缓动曲线。
- **右键菜单动画**：提供淡入、滑动展开、缩放和退场效果，各项入场动画可以使用不同的时长。支持子菜单，并根据一级菜单向上或向下展开的方向调整动画。
- **毛玻璃效果**：为主窗口中的弹窗、命令面板、右键菜单和编辑器补全浮层提供半透明背景，可调整模糊、亮度、对比度和饱和度，也可选择是否保留原背景色及其不透明度。

设置面板分为「背景」「弹窗」「右键菜单」「毛玻璃」四个标签页，提供滑块、开关和曲线选项，也支持手写自定义缓动曲线。调整即时生效并自动保存，支持单项恢复默认、重置全部参数，以及打开测试弹窗预览效果。

## 实现概况

项目使用 JavaScript（脚本语言）和 CSS（样式表），通过 Obsidian 插件接口运行，`main.js` 是直接加载的入口文件。视觉参数主要通过 CSS 变量（可动态调整的样式参数）控制，功能开关通过页面上的样式类启用对应效果。

插件监听页面元素的增删来识别弹窗和菜单的开合，并扩展 Obsidian 的弹窗关闭方法。弹窗或菜单关闭时，会创建一份不可交互的临时界面副本来播放退场动画，动画结束后自动清理。背景效果则跟随实际弹窗的打开状态变化。

## 主要文件

| 文件 | 内容 |
| --- | --- |
| `main.js` | 插件主逻辑，包括默认参数、设置与样式变量的映射、界面变化监听、退场副本管理、设置面板及预览和确认弹窗。 |
| `styles.css` | 背景效果、弹窗与菜单动画、毛玻璃样式、界面层级和设置面板外观。 |
| `manifest.json` | 插件名称、标识符、版本、最低 Obsidian 版本和桌面端支持信息。 |
| `data.json` | 当前保存的用户设置，包括手动调整的动画和视觉参数。 |
